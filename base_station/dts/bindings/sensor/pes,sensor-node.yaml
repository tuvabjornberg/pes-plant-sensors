description: PES sensor node connected to base station via UART

compatible: "pes,sensor-node"

include: base.yaml

properties:
  uart:
    type: phandle
    required: true
    description: UART device used to communicate with the sensor node
